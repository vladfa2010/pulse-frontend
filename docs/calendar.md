# Календарь инвестора

> Блок «Календарь инвестора» на главной странице. Данные загружаются админом вручную через `/admin` → вкладка **Календарь** (или `POST /api/admin/calendar`). Располагается в самом низу `Home.tsx` после всех остальных блоков.

---

## Обзор

Календарь показывает ближайшие корпоративные события по рынку:

- отчётности (МСФО / РСБУ);
- собрания акционеров (СД);
- советы директоров (СА);
- дивиденды;
- другие события.

Данные загружаются единым снапшотом, бэкенд отдаёт плоский список строк, а фронтенд группирует их по дням и типам.

---

## Размещение на главной

Файл: `src/pages/Home.tsx`.

Блок монтируется последним:

```tsx
{/* ==================== INVESTOR CALENDAR ==================== */}
<CalendarBlock portfolio={portfolio} />
```

Также добавлен в комментарий-оглавление в шапке файла:

```
12. CalendarBlock — календарь инвестора
```

См. `docs/home.md` → раздел **Структура**.

---

## Архитектура (frontend)

```
┌─────────────────────────────────────────────┐
│  Home.tsx                                   │
│  └── <CalendarBlock portfolio={portfolio} />│
└───────────────────┬─────────────────────────┘
                    │
        ┌───────────▼───────────┐
        │  CalendarBlock.tsx    │
        │  - лента дат          │
        │  - фильтры            │
        │  - группы событий     │
        └───────┬───────────────┘
                │
    ┌───────────┼───────────┐
    │           │           │
┌───▼────┐ ┌───▼──────┐ ┌───▼────────────┐
│ copy.ts │ │calendarApi.ts│ │calendarMatch.ts│
└─────────┘ └────────────┘ └────────────────┘
```

---

## Типы

Файл: `src/types/calendar.ts`.

```ts
export type EventKind = 'МСФО' | 'РСБУ' | 'СД' | 'СА' | 'Дивиденды' | 'Другое'
export type EventStatus = 'confirmed' | 'expected'

export interface CalendarCompany {
  name: string
  ticker: string
}

export interface CalendarEventGroup {
  title: string
  kind: EventKind
  status: EventStatus
  companies: CalendarCompany[]
}

export interface CalendarDay {
  date: string      // YYYY-MM-DD
  weekday: string   // 'пн'..'вс'
  groups: CalendarEventGroup[]
}

export interface CalendarResponse {
  server_date: string    // YYYY-MM-DD по московскому времени
  generated_at: string   // ISO timestamp загрузки снапшота
  stale: boolean         // true, если данные устарели
  days: CalendarDay[]
}

export interface CalendarAdminEvent {
  date: string      // YYYY-MM-DD
  weekday: string
  title: string
  kind: EventKind
  status: EventStatus
  companies: CalendarCompany[]
  companies_count: number
}
```

---

## API-обёртка

Файл: `src/lib/calendarApi.ts`.

```ts
export async function getCalendar(): Promise<CalendarResponse>
```

- Делает `GET /calendar` через общий `api.ts`.
- Если бэкенд вернул `503 { error: 'calendar_not_loaded' }`, ошибка пробрасывается как есть (`status === 503`, `message === 'calendar_not_loaded'`).
- Любая другая ошибка заворачивается в `calendarCopy.error`.

Кэширование: React Query `queryKey: ['calendar']`, `staleTime: 5 мин`, `gcTime: 30 мин`.

---

## Компонент `CalendarBlock`

Файл: `src/components/CalendarBlock.tsx`.

### Пропсы

| Проп | Тип | Описание |
|------|-----|----------|
| `portfolio` | `PortfolioTag[]` | Теги пользователя; нужны для подсветки компаний из портфеля. |

### Состояния

| Состояние | Когда показывается |
|-----------|--------------------|
| `isLoading` | `CalendarSkeleton` — серый плейсхолдер. |
| `isError && status === 503 && message === 'calendar_not_loaded'` | Блок полностью скрыт (снапшот ещё не загружен). |
| `isError` другой | Карточка с текстом `calendarCopy.error` и кнопкой «Повторить». |
| `data` получен | Полный UI: шапка, лента дат, фильтры, события. |
| `data.stale === true` | Жёлтый баннер «Данные устарели». |

### Лента дат

- Горизонтальный scrollable список дней из ответа бэкенда.
- Поддерживает drag мышью и touch.
- При клике по дню она становится выделенной, скролл подстраивается, чтобы выбранная дата оказалась по центру.
- Текущий день (`server_date`) обведён полупрозрачной голубой рамкой.
- Выбранный день — голубая подсветка + glow.
- Под каждой датой — точки цветами типов событий.
- Если в день есть компания из портфеля пользователя — внизу длинная голубая/зелёная полоса.
- Выходные дни отображаются приглушённым цветом.

### Выбор даты по умолчанию

Логика в `findDefaultDate(days, serverDate)`:

1. Если в снапшоте есть `server_date` — выбран сегодня.
2. Иначе — ближайшая будущая дата.
3. Иначе — последняя доступная дата.

### Фильтры

Кнопки-фильтры по типу события:

- `Все` (все группы);
- `МСФО`, `РСБУ`, `СД`, `СА`, `Дивиденды`, `Другое`.

Выбранный фильтр сохраняется в `localStorage` (`pulse_calendar_filter`).

Если для выбранного дня нет событий выбранного типа — показывается `calendarCopy.emptyFilter`.

### Группы событий

Каждая группа рендерится карточкой:

- Бейдж типа события цветом `KIND_COLORS`.
- Заголовок события.
- Бейдж статуса: `confirmed` → «подтверждено» (зелёный), `expected` → «ожидается» (жёлтый).
- Список компаний с тикерами.
- Компании из портфеля пользователя подсвечиваются голубым и помечаются бейджем «в портфеле».
- Если компаний больше 6 — показываются первые 6 и кнопка «ещё N ▾»; при раскрытии — «свернуть ▴».

### Подсветка портфеля

Логика в `src/lib/calendarMatch.ts`.

Правила совпадения (первое выполнившееся):

1. Точное совпадение тикера: `tag_id.toUpperCase() === company.ticker`.
2. Точное совпадение нормализованного названия: `normalizeName(tag_name) === normalizeName(company.name)`.

Нормализация: lower-case, trim, удаление кавычек/ёлочек, удаление юридических форм `ПАО` / `АО`.

> Пока **нет** fuzzy/substring matching. Тикеры работают надёжнее названий.

### Аналитика

| Событие | Когда |
|---------|-------|
| `calendar_match_rate` | Один раз за сессию: сколько компаний календаря есть в портфеле. |
| `calendar_day_select` | Клик по дате в ленте. |
| `calendar_filter_change` | Переключение фильтра. |
| `calendar_group_expand` | Раскрытие длинного списка компаний. |
| `calendar_company_click` | Клик по компании (передаётся `ticker`, `kind`, `in_portfolio`). |
| `calendar_refresh_click` | Клик по кнопке «Обновить». |

### SSE-обновление

Файл: `src/hooks/useSseNews.ts`.

Слушает событие `calendar:refresh` от бэкенда и инвалидирует кэш:

```ts
queryClient.invalidateQueries({ queryKey: ['calendar'] })
```

Это позволяет обновлять календарь у всех активных клиентов сразу после админской загрузки нового снапшота.

---

## Тексты

Файл: `src/lib/copy.ts`.

```ts
export const calendarCopy = {
  title: 'Календарь',
  subtitle: 'Отчётности и события по рынку',
  updated: 'Обновлено',
  refresh: 'Обновить',
  retry: 'Повторить',
  error: 'Не удалось загрузить календарь',
  source: 'Источник: данные загружаются администратором · ожидается = прогнозная дата',
  today: 'СЕГОДНЯ',
  nearest: 'Сегодня событий нет — ближайшие:',
  stale: 'Данные устарели',
  emptyDay: 'На этот день событий нет',
  emptyFilter: 'Событий этого типа нет',
  filters: { all: 'Все', 'МСФО': 'МСФО', ... },
  status: { expected: 'ожидается', confirmed: 'подтверждено' },
  portfolioBadge: 'в портфеле',
  expand: (n: number) => `ещё ${n} ▾`,
  collapse: 'свернуть ▴',
  eventCounter: (events, companies, yours) => `${events} событий · ${companies} эмитентов${yours > 0 ? ` · ${yours} ваших` : ''}`,
}
```

---

## Стили

- Карточка блока — `Liquid Glass`: полупрозрачный фон, блюр, тонкая белая рамка.
- Цвета типов событий (`KIND_COLORS`):

| Тип | Цвет |
|-----|------|
| МСФО | `#00D4FF` |
| РСБУ | `#A78BFA` |
| СД | `#FBBF24` |
| СА | `#FB7185` |
| Дивиденды | `#34D399` |
| Другое | `#9CA3AF` |

---

## Edge cases

- **Снапшот не загружен** — блок не рендерится (503 не показывается пользователю).
- **Пустой выбранный день** — текст «На этот день событий нет».
- **Нет событий выбранного типа** — текст «Событий этого типа нет».
- **Сегодня нет событий, но выбрана другая дата** — показывается подсказка «Сегодня событий нет — ближайшие:».
- **Данные устарели** (`stale === true`) — жёлтый баннер; бэкенд также шлёт админам Telegram-алерт раз в сутки.
- **Ошибка сети** — карточка с кнопкой повтора; лента дат остаётся кликабельной после успешного retry.

---

## Админка: вкладка «Календарь»

Файл: `src/pages/admin/CalendarTab.tsx`.

Вкладка состоит из двух частей:

1. **Модуль загрузки снапшота** — загрузка всего календаря из JSON-файла.
2. **Таблица событий** — просмотр, фильтрация, создание, редактирование и удаление отдельных групп событий.

### Загрузка снапшота

- Показывает текущий статус: загружен ли снапшот, сколько дней и компаний.
- Кнопка **Загрузить снапшот** открывает модалку.
- В модалке:
  - `input type="file"` для JSON;
  - поддерживается три формата:
    - сырой `investmint_calendar.json` (frontend парсит сам);
    - `smartlab_calendar.json` — массив объектов `{ date: "ДД.ММ.ГГГГ", title: "TICKER: ..." }`;
    - готовый `{ days: [...] }`.
  - **Предпросмотр** — сразу после выбора файла показывается статистика: количество дней, событий, компаний и диапазон дат. Файл не уходит на сервер, пока админ не нажмёт «Загрузить».
- После успешной загрузки вызывается `POST /api/admin/calendar`, модалка закрывается, статус обновляется.

#### TODO / Известные ограничения

> **Год в raw-формате не указан.**
> Парсер вычисляет год эвристикой: месяц события < текущего месяца → следующий год. Это надёжно для " future-looking" снапшотов, но может ошибаться на исторических или нестандартных файлах.
>
> **Рекомендуемое улучшение:** поддержать год явно в строке даты (`"1 января 2027 пн"`) или добавить поле "Год снапшота" в форму загрузки. При наличии года в файле эвристика должна отключаться.

#### Парсинг `smartlab_calendar.json`

Файл: `src/pages/admin/CalendarTab.tsx` → `parseSmartlabCalendar`.

**Ожидаемая структура:**

```json
[
  { "date": "28.08.2026", "time": "10:30", "title": "MGKL: Трансляция о результатах МСФО за 1п 2026г", "text": "..." },
  { "date": "31.08.2026", "time": "", "title": "BELU: СД рассмотрит вопрос о промежуточных дивидендах", "text": "..." }
]
```

**Логика:**

1. **Дата** — парсится из `ДД.ММ.ГГГГ`, год берётся прямо из файла (никакой эвристики).
2. **Тикер** — вытаскивается из начала `title` по паттерну `TICKER:` или `TICKER - `. Если тикера нет — запись пропускается (в консоль пишется предупреждение).
3. **Название компании** — временно совпадает с тикером (`name === ticker`). Это ограничение формата smartlab: там нет полноценного названия компании.
4. **Тип** — определяется `detectKind` по оставшемуся тексту заголовка (`МСФО`, `РСБУ`, `СД`, `СА`, `Дивиденды`, `Другое`).
5. **Статус** — `detectStatus` (`confirmed`/`expected`).
6. **Группировка** — события с одинаковой датой, заголовком и типом объединяются; компании (тикеры) дедуплицируются.

> **Важно:** smartlab-записи без тикера в начале `title` игнорируются. Если в файле есть события без тикера, их нужно либо доработать вручную, либо добавлять через форму редактора событий.

#### Парсинг raw-формата `investmint_calendar.json`

Файл: `src/pages/admin/CalendarTab.tsx`.

Для каждой строки raw-формата:

1. **Дата** — парсится из строки вида `"1 января пн"`.
   - Год **не указан в файле**, поэтому вычисляется эвристикой:
     - берётся текущий месяц;
     - если месяц события строго меньше текущего → год = текущий + 1;
     - иначе → текущий год.
   - Это позволяет корректно загружать снапшоты, охватывающие следующий год (например, январь 2027), без хардкода.

2. **Тип события (`detectKind`)** — определяется по ключевым словам в порядке приоритета:
   - `совет директоров` или отдельное слово `сд` → `СД`;
   - `госа`, `воса`, `собрание акционеров`, `собрание` → `СА`;
   - `мсфо` → `МСФО`;
   - `рсбу` → `РСБУ`;
   - `дивиденд` → `Дивиденды` (проверка идёт после `СД`, чтобы фразы «СД по дивидендам» попадали в `СД`);
   - всё остальное → `Другое`.

3. **Статус (`detectStatus`)**:
   - `ожидается` или `предварительно` → `expected`;
   - остальное → `confirmed`.

4. **Компании** — каждый токен разбивается по регулярке:
   - буква рейтинга + название + тикер латиницей (`COMPANY_RE`).

#### Почему именно `/api/admin/calendar`?

В проекте два семейства админ-роутов:

| Семейство | Путь | Примеры |
|-----------|------|---------|
| Legacy | `/admin/...` | `/admin/llm-dashboard`, `/admin/tags`, `/admin/backfill` |
| Новые | `/api/admin/...` | `/api/admin/users`, `/api/admin/plans`, `/api/admin/calendar` |

`adminApi` в `src/lib/api.ts` использует базовый URL без `/api`, поэтому для новых роутов нужно указывать полный путь **с префиксом `/api/admin/`**:

```ts
// правильно
await adminApi.post('/api/admin/calendar', { days })

// неправильно (404 — такого legacy-роута нет)
await adminApi.post('/admin/calendar', { days })
```

---

### Таблица событий

Файл: `src/components/admin/CalendarEventsTable.tsx`.

Компонент получает callbacks `onEdit`, `onAdd` и `refreshSignal`.

**Функциональность:**

- Загружает группы событий через `GET /api/admin/calendar/events?search=&kind=&status=&limit=25&offset=`.
- **Поиск** по заголовку события, названию компании или тикеру.
- **Фильтры** по типу (`МСФО`, `РСБУ`, `СД`, `СА`, `Дивиденды`, `Другое`) и статусу (`подтверждено` / `ожидается`).
- **Пагинация** по 25 записей на страницу.
- **Клик по строке** → редактирование.
- **Кнопка «Добавить»** → создание.
- **Иконка корзины** → удаление с подтверждением `window.confirm`.

**Колонки:**

| Колонка | Описание |
|---------|----------|
| Дата | `date` + `weekday`. |
| Заголовок | `title`. |
| Тип | `kind`. |
| Статус | бейдж: зелёный `confirmed`, жёлтый `expected`. |
| Компаний | `companies_count`. |
| Действия | удалить. |

---

### Редактор события

Файл: `src/components/admin/CalendarEventModal.tsx`.

Модалка работает в двух режимах: **создание** (`event === null`) и **редактирование**.

**Поля формы:**

| Поле | Описание |
|------|----------|
| Дата | `input type="date"`. При изменении автоматически вычисляется день недели. |
| День недели | текстовое поле, можно отредактировать вручную. |
| Заголовок | текст события. |
| Тип | селект из `EventKind`. |
| Статус | `confirmed` / `expected`. |
| Компании | список пар `название + тикер`, можно добавлять и удалять строки. |

**Логика редактирования:**

1. При открытии модалки в режиме редактирования делается `GET /api/admin/calendar/events/:date/:title/:kind`.
2. В ответе приходит полная группа со списком компаний.
3. Форма заполняется полученными данными.
4. При сохранении отправляется `PUT` на тот же путь с телом нового состояния.
5. Редактор позволяет изменить `date`/`title`/`kind`; бэкенд удалит старую группу и создаст новую.

**Валидация на клиенте:**

- Обязательны дата, заголовок, тип, статус.
- Должна быть хотя бы одна заполненная компания (`name` + `ticker`).
- Тикеры внутри одной группы не должны повторяться (проверка `toUpperCase()`).

**Сохранение:**

- `POST /api/admin/calendar/events` — для новой группы.
- `PUT /api/admin/calendar/events/:date/:title/:kind` — для существующей.

После успешного сохранения:

- модалка закрывается;
- обновляется статус снапшота в `CalendarTab`;
- таблица перезагружается через `refreshSignal`;
- SSE `calendar:refresh` автоматически обновит календарь у всех активных клиентов.

---

### Связанные файлы

| Файл | Назначение |
|------|-----------|
| `src/components/CalendarBlock.tsx` | Основной UI. |
| `src/lib/calendarApi.ts` | Запрос `GET /calendar`. |
| `src/lib/calendarMatch.ts` | Сопоставление с портфелем. |
| `src/lib/copy.ts` | Тексты календаря. |
| `src/types/calendar.ts` | TypeScript-типы. |
| `src/hooks/useSseNews.ts` | SSE-инвалидция кэша. |
| `src/pages/Home.tsx` | Размещение блока на главной. |
| `src/pages/admin/CalendarTab.tsx` | Вкладка админки: снапшот + таблица событий. |
| `src/components/admin/CalendarEventsTable.tsx` | Таблица событий с фильтрами и пагинацией. |
| `src/components/admin/CalendarEventModal.tsx` | Редактор/создание группы события. |
| `docs/home.md` | Общая структура главной. |
